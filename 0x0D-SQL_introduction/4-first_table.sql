-- Creates a table called first_table with columns id and name in your MySQL server.
CREATE TABLE IF NOT EXISTS `first_table` (
  `id` INT, -- Column for storing numeric IDs
  `name` VARCHAR(256) -- Column for storing names as text
);
