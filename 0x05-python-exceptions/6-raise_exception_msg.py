def raise_exception_msg(message=""):
    """Raises a NameError with the specified message.

    Args:
        message (str, optional): The message to include in the NameError. Defaults to an empty string.
    """

    raise NameError(message)
